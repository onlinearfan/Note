*)window search or win+ run diye cmd open kora jai,
*)right click property show, font,text color change krte pari,
*)color dile defalut color asbe,
*)color ?/ = list color asbe, color F0 = black and white,
*)cls = clear type,
*)dir = kno folder er bitor ki ase dekhte,
*)dir/a = hidden soho dekhte,
*)mkdir newFolderName = new folder banate,
*)rmdir nameFolder = empty folder delete krte,
*)rmdir /s folderName = delete folder with file, (y/n)
*)cd .. / cd ../.. = back + dubble folder back,
*)cd folderName/folder name with space, = go to folder or open folder
*)cd tab = sob folder ekta kore asbe,
*)cd s + tab = s er sob folder ekta kore asbe,
*)up arrow down arrow = back and forward cmd command
*)touch file.txt
*)echo>ar.txt = file create,
*)echo "file inner text">b.txt = new file with inner text,
*)arfan.txt = file open in notepad,
*)echo arfan> arfan.txt = file text override hobe, file create thkle,
*)echo arfan>>arfan.txt = file text appand hobe,
*)move dhaka bangladesh = ekta folder er bitor arekta move kora,
*)copy a1.txt a2.txt = ekta file er text onno tai override hobe,
*)move a1.txt a2.txt = move file er text override hobe and move file delete hobe,
*)dir>>a.txt = eita sob dir info a.txt te print krbe,
*)del a.txt / del a2.txt,ar.txcle = delete korbe,
*)type a.txt = eita tar bitor lekha cmd te show krbe,
*)clt + right/left arrow dile folder kore arrow jave,
*)dir *.png = only png show krbe,
*)wmic logicaldisk get name = pc partition folder name show krbe,
*)folder>tree = folder er structure show krbe,